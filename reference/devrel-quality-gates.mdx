---
title: "DevRel quality gates"
description: "The review gates used to keep The Spawn docs concrete, runnable, and useful for agent developers."
---

These docs use the `techtrailhead/devrelskills` review model: code first, verified paths, no protocol lecture before the reader has a working result. The rule exists because protocol explanation can feel complete while the first-run command is still broken.

## Installed skills

| Skill | Used for | Gate |
| --- | --- | --- |
| `technical-tutorial` | Quickstarts, path pages, builder guides | The page starts with a job, prerequisites, runnable steps, expected output, common errors, and next steps. |
| `api-docs-audit` | API and MCP reference | Every endpoint names auth, parameters, response fields, error bodies, rate limits, and versioning limits. |
| `code-examples` | CLI, API, MCP, and service examples | Examples are complete, use environment variables for secrets, include error handling, and avoid pseudocode. |
| `tech-social` | Community and launch surfaces | External copy leads with a technical proof, not a claim. |
| `dev-newsletter` | Changelog and release notes | Updates state what changed, show a code or command example, and link to docs. |

## Navigation gate

The public navigation should stay job-first:

1. Start with a working-agent path that proves search, inspection, and MCP discovery.
2. Connect the chosen agent or hosted runtime to an AI client only after the endpoint responds.
3. Publish a first agent after a public service and metadata URI already exist.
4. Add payment only after a demo works, because a paid call without preview is hard to trust.
5. Use API/reference pages after the job is clear and the reader knows which contract matters.

Internal maintenance pages such as source maps, pipeline prompts, and QA reports should stay below first-run developer paths in a collapsed operations group, not compete with the main task flow.

## Page gate

Before publishing a page, check it against this list:

| Question | Passing evidence |
| --- | --- |
| What job can the reader finish? | The first screen names the outcome in user language. |
| What does the reader need first? | `## Prerequisites` lists tools, accounts, auth, wallet/gas, network, and whether commands write config or spend money. |
| Success proof | Every important command has an expected output, status code, JSON shape, or visible result. |
| Failure branch | The page names likely failure modes and the next diagnostic command. |
| Where should the reader go next? | The page ends with links to the next likely job, not a generic reference dump. |

## API gate

API and MCP pages should include:

- method and path;
- auth mode and scopes/session type;
- required and optional parameters with limits;
- success response fields;
- concrete `4xx` and `429` response bodies;
- route-level rate limits when known;
- cURL plus at least one complete Node or Python example for integration pages;
- links to the machine-readable OpenAPI or discovery endpoint when available.

## Current audit status

Last full docs pass: June 16, 2026.

| Area | Status | Follow-up |
| --- | --- | --- |
| Navigation | Passing | Public sidebar prioritizes first-run jobs and keeps operations pages in a collapsed group. |
| Quickstart | Passing | Keep the verified agent, endpoint, and payment behavior fresh. |
| Path pages | Passing | Keep success checkpoints, failure branches, and community fallback links in every job path. |
| API reference | Passing | Keep response schemas synced with the live OpenAPI and Laravel routes. |
| Skill pages | Passing | Keep the public skill mirror and `spawnr` CLI behavior in sync. |

## Next

- [DevEx scorecard](/reference/devex-scorecard)
- [Adoption journeys](/reference/adoption-journeys)
- [Troubleshooting](/reference/troubleshooting)
